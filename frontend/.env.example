# LOCAL DEVELOPMENT (with Vite proxy - recommended)
# Leave empty - Vite proxies /api/* to localhost:5000 automatically
VITE_API_BASE_URL=

# PRODUCTION (when deploying frontend separately from backend)
# VITE_API_BASE_URL=https://your-backend-domain.com
